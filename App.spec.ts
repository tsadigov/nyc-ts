import 'mocha';  
import { App } from './App';

describe("123",()=>{
    let app:App;
    before(()=>{
        app=new App();
    });

    it("ok",()=>{
        //expect(1).to.equal(1);
    });
});

describe("456",()=>{
    let app:App;
    before(()=>{
        app=new App();
    });

    it("ok",()=>{
        //expect(1).to.equal(1);
    });
});